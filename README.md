# Agnes AI 图片生成插件

[![KiraAI](https://img.shields.io/badge/KiraAI-插件-blue)](https://github.com/znq19/KiraAI)
[![版本](https://img.shields.io/badge/version-1.3.0-green)]()
[![Python](https://img.shields.io/badge/Python-3.10+-yellow)]()

> 让 KiraAI 的 Bot 能通过 Agnes AI 快速生成高质量（大概是吧）图片，**自动发送到聊天**，全程无需手动操作。

## 它能做什么

- 用户说 "画一只猫" → Bot 自动生成图片 → 直接发到群/私聊
- 用户说 "发张你的自拍" → Bot 用自己的形象参考图，生成角色图 → 自动发送
- 支持文生图、图生图、角色形象图三种模式

## 特性

- 🎨 **4 种风格** × **4 档尺寸** × **8 种宽高比**：动漫 / 写实 / 油画 / 水彩，1K~4K 高清档位，正方形到超宽屏
- 🖼️ **4K 超清输出**：基于 Agnes Image 2.5 Flash，支持 1K / 2K / 3K / 4K 档位（最高 4096×4096），用户说"高清/超清/4K"即可生成
- 📐 **自定义宽高**：可在插件设置里填自定义宽/高（如 1024×768），用户要求精确分辨率时使用
- 🤖 **角色形象图**：配置一张 Bot 角色参考图，用户说 "你长什么样" 就自动图生图
- 📨 **合并转发**：多张图用 QQ 合并转发发送，不刷屏
- 🔄 **失败自动回退**：合并转发失败 → 自动切换逐张发送
- 🔁 **智能重试**：API 繁忙时自动等几秒重试，最多 3 次
- 📢 **错误详情回传**：生成失败时把具体报错（HTTP 状态码 + 原因）回传给 AI，让 Bot 能准确告知用户问题所在
- 💾 **缓存管理**：生成图自动保存，超出上限自动清理旧文件
- 🌐 **代理支持**：配置 HTTP 代理即可

## 安装

1. 下载插件文件夹
2. 放到 KiraAI 的 `data/plugins/` 目录下：

```text
KiraAI/
  data/
    plugins/
      agnes_image_gen/     ← 放这里
        main.py
        manifest.json
        schema.json
        requirements.txt
```

3. 打开 KiraAI WebUI → 插件管理 → 找到 "Agnes AI 图片生成" → 启用
4. **填写 API Key**（必填）：

> API Key 是 `sk-` 开头的密钥，从 Agnes AI 获取。没有的话去 [Agnes AI](https://apihub.agnes-ai.com) 注册。（目前纯免费白嫖，如你发现你使用它国际站的base url和key无法生成，可换用中国站：https://platform.agnes-ai.cn/ ）

## 配置说明

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| API Key | Agnes AI 密钥（必填） | 空 |
| 模型 | 生成模型名称 | agnes-image-2.5-flash |
| 默认尺寸档位 | 1K / 2K / 3K / 4K | 2K |
| 默认宽高比 | 1:1 / 3:4 / 4:3 / 16:9 / 9:16 / 2:3 / 3:2 / 21:9 | 16:9 |
| 自定义宽度 / 高度 | 精确分辨率（宽×高，如 1024×768），0=不启用 | 0 / 0 |
| 默认风格 | 动漫 / 写实 / 油画 / 水彩 | 动漫 |
| 单次最大张数 | 一次生成几张 | 4 |
| 异步生成 | 开启后工具快速返回、后台生成、完成自动发送并通知 AI | **开启** |
| 角色形象参考图 | Bot 角色自身的形象参考图路径 | 自动读取 KiraAI 设置 |
| 合并转发 | 多张图是否合并转发（仅 QQ） | 开启 |
| 保留生成图片 | 发送后是否保留本地文件 | 开启 |
| 缓存上限 | 最多保留多少张本地图 | 100 |
| 请求超时 | API 请求超时（4K 生成较慢，建议 ≥180 秒） | 180 秒 |

> 💡 **角色形象参考图**：留空会自动读取 KiraAI 系统设置的 selfie 图片。填了就用你填在插件这的。

## 使用示例

### 文生图

```
用户：帮我画一只在樱花树下的白猫，动漫风
Bot： [调用工具] → [生成] → [发送] → "樱花树下的白猫来啦"
```

### 高清 / 4K 画质

```
用户：画一张 4K 超清的城市夜景壁纸，横屏的
Bot： [size=4K, ratio=16:9] → [生成] → [发送] → "4K 城市夜景壁纸来啦"
```

> 尺寸档位：1K（约 1024px）/ 2K（约 2048px）/ 3K（约 3072px）/ 4K（约 4096px），
> 配合宽高比 1:1、3:4、4:3、16:9、9:16、2:3、3:2、21:9。
> 需要精确分辨率时，可在插件设置中配置「自定义宽度/高度」（如 1024×768）。

### 角色形象图

```
用户：你长什么样？发张自拍看看
Bot： [use_selfie=true] → [用角色参考图生成] → [发送] → "喏，这就是我"
```

### 图生图

```
用户：基于这张图，帮我换个赛博朋克风格的背景
Bot： [reference_image_url=用户发的图的URL] → [生成] → [发送]
```

## 依赖

- Python 3.10+
- aiohttp

KiraAI 会在首次加载时自动安装依赖。

## License

AGPLv 3.0

<details>
<summary>📜 更新日志</summary>

### v1.3.0（2026-09-02）

**✨ 新增**
- 🖼️ **4K 超清档位**：新增尺寸档位 1K / 2K / 3K / 4K（最高 4096×4096），配合 8 种宽高比（1:1、3:4、4:3、16:9、9:16、2:3、3:2、21:9）。用户说"高清/超清/4K"即可生成高分辨率图片
- 📐 **宽高比参数**：工具新增 `ratio` 参数，竖图/横图/宽屏/壁纸等需求可精确指定比例；插件设置新增「默认宽高比」
- 📏 **自定义宽高**：插件设置新增「自定义宽度/高度」两个配置项（分开填写），填了之后用户要求精确分辨率（如 1920×1080）时使用
- 📢 **错误详情回传**：API 调用失败时，把具体报错（HTTP 状态码、错误原因、超时提示等）回传给 AI，Bot 能准确告知用户问题所在，不再瞎猜
- ⏱️ **超时上限提升**：请求超时范围 5~300 秒 → 5~360 秒，默认 120 → 180 秒

**⚙️ 变更**
- 默认模型 `agnes-image-2.1-flash` → `agnes-image-2.5-flash`（新一代图像模型，支持 1K~4K 档位，价格与 2.1 相同，当前全免费）
- 默认尺寸 `1024x1024` → `2K`，默认宽高比 `1:1` → `16:9`
- 移除预设的历史精确尺寸选项（1024x1024 等），需要精确尺寸时用「自定义宽度/高度」配置
- API 地址配置项 hint 补充中国站地址（platform.agnes-ai.cn）

### v1.2.1（2026-08-29）

**✨ 新增**
- 🎨 **插件图标**：新增 512×512 圆角透明底 PNG 图标（热带水果市场少女），manifest 增加 `icon` 字段，WebUI 插件列表显示图标

### v1.2.0（2026-08-08）

**✨ 新增**
- ⚡ **异步生成（默认开启）**：新增配置 `async_generate`（生成设置，默认开）。开启后工具调用**快速返回**，AI 先回复用户"正在生成"，图片在后台生成，**完成后自动发送到聊天**，并触发 AI 补一句"图片已生成"（与对话自然衔接）。期间不阻塞 AI 回复、不触发工具调用超时；同一会话重复请求会提示"正在生成中"；插件卸载自动取消后台任务。关闭则保持原同步行为（工具等生成完再返回）

### v1.1.0（2026-08-02）

**修复**
- 修复本地参考图路径解析失败：LLM 传入的 `data/temp/xxx.jpg` 会被错误拼成 `data/data/temp/xxx.jpg`（data 前缀重复）。现在自动尝试多种路径写法（相对 data 目录 / 相对根目录 / 绝对路径）
- 修复参考图解析失败时**静默降级为文生图**的问题：之前参考图找不到会假装"图生图成功"（实际是无参考生成的图）。现在会显式报错并提示正确的路径格式
- 参考图解析逻辑前移到工具层，解析失败直接返回错误，不再假成功
- **修复插件配置的「默认尺寸 / 默认风格」不生效**：之前 LLM 不传这两个参数时会走代码里的硬编码默认值，插件设置里的默认值被跳过。现在未传时正确使用插件配置的默认值

**改进**
- **支持多张参考图**：`reference_image_url` 可用英文逗号分隔传多张（最多 4 张），自动逐张解析后一起传给 API
- **角色形象图 + 参考图可叠加**：`use_selfie=true` 可与 `reference_image_url` 同时使用（如"用你的形象 + 这张图的场景"），形象参考图自动放在首位
- 新增「角色形象图」模式：配置 Bot 角色参考图后，用户说"你长什么样""发张自拍"即可自动图生图
- 角色参考图留空时自动读取 KiraAI 系统设置的 selfie 图
- 移除不支持的 `response_format` 参数（修复 400 报错）
- API 繁忙（429/5xx）时自动等待重试，最多 3 次；4xx 参数错误不再无意义重试
- 图生图工具说明优化：明确告诉 AI 用消息上下文的 `file_path` 作为参考图

### v1.0.0（2026-07-24）

- 首个版本：文生图 / 图生图，4 种风格 × 5 种尺寸
- 多图合并转发（QQ）+ 失败自动回退逐张发送
- 缓存管理与自动清理，支持 HTTP 代理

</details>
